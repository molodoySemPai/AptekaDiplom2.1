@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        //Перенаправляем пользователя на каталог сразу же
        Nav.NavigateTo("/catalog", forceLoad: true);
    }
}